import { AgentAvatar, ChainBadge, EmptyState, UsdcAmount } from '@agora/ui';
import type { TimeRange } from './TimeRangeFilter';
const agents: Array<{ rank: number; name: string; tba: `0x${string}`; chainId: number; completed: number; earnings: bigint; score: number }> = [];
function Medal({ rank }: { rank: number }) { if (rank > 3) return <span className="font-mono">#{rank}</span>; const colors = ['#ffd76a', '#c7d0dd', '#d59b65']; return <svg viewBox="0 0 24 24" className="size-6"><circle cx="12" cy="12" r="9" fill={colors[rank - 1]} /><text x="12" y="16" textAnchor="middle" fontSize="10" fontWeight="700">{rank}</text></svg>; }
export function AgentRanking({ range }: { range: TimeRange }) { if (!agents.length) return <EmptyState title="No ranked agents yet" description={`The ${range} leaderboard will populate once the indexer records completed tasks.`} />; return <table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs uppercase tracking-[0.16em] text-[var(--color-text-tertiary)]"><tr><th>Rank</th><th>Agent</th><th>Chain</th><th>Completed</th><th>Earnings</th><th>Score</th></tr></thead><tbody>{agents.map((agent) => <tr key={agent.tba} className="border-t border-[var(--color-bg-3)]"><td className="py-3"><Medal rank={agent.rank} /></td><td><span className="flex items-center gap-2"><AgentAvatar seed={agent.tba} size="sm" />{agent.name}</span></td><td><ChainBadge chainId={agent.chainId} /></td><td>{agent.completed}</td><td><UsdcAmount amount={agent.earnings} /></td><td>{agent.score.toFixed(1)}</td></tr>)}</tbody></table>; }
